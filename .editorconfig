root = true

# No charset rule: this repository has a mix of BOM and BOM-less .cs files.
# A single charset value cannot express "preserve each file's existing encoding",
# so encoding is deliberately not enforced here. Normalizing it is a separate change.
[*]
indent_style = space
indent_size = 4
end_of_line = crlf
trim_trailing_whitespace = true
insert_final_newline = true

[*.cs]
# C# Coding Conventions
csharp_style_namespace_declarations = file_scoped:suggestion
csharp_style_prefer_primary_constructors = true:suggestion
dotnet_style_prefer_collection_expression = true:suggestion
csharp_style_prefer_pattern_matching = true:suggestion
csharp_style_var_for_built_in_types = true:suggestion
csharp_style_var_when_type_is_apparent = true:suggestion
csharp_style_var_elsewhere = true:suggestion
